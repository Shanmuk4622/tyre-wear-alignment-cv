# Native app code, launcher, requirements and docs stay in Git.
/.vendor/
/.cache/
/checkpoints/
/results/

# Reopen data only for the incoming placeholder. Print targets are regenerated.
!/data/
/data/*
!/data/incoming/
/data/incoming/*
!/data/incoming/.gitkeep
